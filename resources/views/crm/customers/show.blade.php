@extends('layouts.app') @section('title',$customer->name) @section('content')<div class="flex justify-between"><h1 class="text-2xl font-semibold">{{$customer->name}}</h1><div><a class="btn" href="{{route('crm.customers.edit',$customer)}}">Edit</a><a class="btn-primary" href="{{route('crm.purchases.create',$customer)}}">Transaksi Baru</a></div></div><p>{{$customer->address}}</p><p>Kontak: @foreach($customer->contacts as $c){{$c->type}}: {{$c->value}} @endforeach</p><h2 class="mt-5 font-semibold">Transaksi</h2><table class="w-full">@foreach($customer->purchases as $p)<tr><td><a class="link" href="{{route('crm.purchases.show',$p)}}">{{$p->purchase_number}}</a></td><td>{{$p->purchased_at?->format('d/m/Y')}}</td><td>Rp {{number_format($p->total_amount)}}</td></tr>@endforeach</table><h2 class="mt-5 font-semibold">Histori</h2>@foreach($customer->histories as $h)<div>{{$h->created_at?->format('d/m/Y H:i')}} — {{$h->user?->name}} — {{$h->action}} {{$h->note}}</div>@endforeach@endsection
