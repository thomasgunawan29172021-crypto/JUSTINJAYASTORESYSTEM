@extends('layouts.app') @section('title','Edit Pelanggan') @section('content')<h1 class="text-2xl font-semibold">Edit Pelanggan</h1><form method="POST" action="{{route('crm.customers.update',$customer)}}" class="space-y-3">@csrf @method('PUT')<input name="name" value="{{old('name',$customer->name)}}" class="input" required><input name="address" value="{{old('address',$customer->address)}}" class="input"><input name="source" value="{{old('source',$customer->source)}}" class="input"><select name="branch_id" class="input">@foreach($branches as $b)<option value="{{$b->id}}" @selected(old('branch_id',$customer->branch_id)==$b->id)>{{$b->name}}</option>@endforeach</select><textarea name="notes" class="input">{{$customer->notes}}</textarea><button class="btn-primary">Simpan</button></form>@endsection
