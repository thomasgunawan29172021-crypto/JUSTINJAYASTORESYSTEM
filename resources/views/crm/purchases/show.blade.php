@extends('layouts.app') @section('title',$purchase->purchase_number) @section('content')<h1 class="text-2xl font-semibold">{{$purchase->purchase_number}}</h1><p>Pelanggan: <a class="link" href="{{route('crm.customers.show',$purchase->customer)}}">{{$purchase->customer->name}}</a></p><table class="w-full">@foreach($purchase->items as $i)<tr><td>{{$i->product_name}}</td><td>{{$i->imei}}</td><td>{{$i->qty}} × Rp {{number_format($i->unit_price)}}</td></tr>@endforeach</table><p class="font-semibold">Total Rp {{number_format($purchase->total_amount)}}</p>@foreach($purchase->histories as $h)<div>{{$h->created_at}} — {{$h->user?->name}} — {{$h->action}}</div>@endforeach@endsection
