@extends('layouts.app') @section('title','Transaksi Baru') @section('content')<h1 class="text-2xl font-semibold">Transaksi — {{$customer->name}}</h1><form method="POST" action="{{route('crm.purchases.store',$customer)}}" class="space-y-3">@csrf<select name="branch_id" class="input">@foreach(auth()->user()->branches->push(auth()->user()->branch)->unique('id') as $b)<option value="{{$b->id}}">{{$b->name}}</option>@endforeach</select><input type="date" name="purchased_at" value="{{date('Y-m-d')}}" class="input" required><div class="item flex gap-2"><select name="items[0][product_id]" class="input"><option value="">Produk bebas</option>@foreach($products as $p)<option value="{{$p->id}}">{{$p->name}}</option>@endforeach</select><input name="items[0][product_name]" placeholder="Nama produk jika bebas" class="input"><input name="items[0][qty]" type="number" value="1" min="1" class="input"><input name="items[0][unit_price]" type="number" value="0" min="0" class="input"><input name="items[0][imei]" placeholder="IMEI/seri" class="input"></div><textarea name="notes" class="input" placeholder="Catatan"></textarea><button class="btn-primary">Simpan</button></form>@endsection
