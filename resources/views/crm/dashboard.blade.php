@extends('layouts.app') @section('title','Laporan CRM') @section('content')<h1 class="text-2xl font-semibold">Laporan CRM</h1><form class="my-3"><input type="date" name="from" value="{{$from->format('Y-m-d')}}" class="input"><input type="date" name="to" value="{{$to->format('Y-m-d')}}" class="input"><button class="btn">Filter</button></form><div class="grid gap-3 md:grid-cols-3"><div class="card">Pelanggan baru: {{$baruCount}}</div><div class="card">Pelanggan repeat: {{$repeatCount}}</div></div><h2 class="mt-5 font-semibold">VIP</h2>@foreach($vip as $c)<div>{{$c->name}} — Rp {{number_format($c->purchases_sum_total_amount??0)}}</div>@endforeach<h2 class="mt-5 font-semibold">Produk dibeli ulang</h2>@foreach($repeatProducts as $p)<div>{{$p->product_name}} — {{$p->total_dibeli}}</div>@endforeach<h2 class="mt-5 font-semibold">Respon reminder</h2>@foreach($reminderStats as $stage=>$stats)<div>{{$stage}}: @foreach($stats as $s){{$s->status}} {{$s->total}} @endforeach</div>@endforeach@endsection
